[project]
name = "acars-collector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.0,<1",
    "asyncpg>=0.29.0,<1",
    "python-dotenv>=1.0.0,<2",
    "playwright>=1.61.0,<2",
]

[project.optional-dependencies]
dev = []